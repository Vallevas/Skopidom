#!/usr/bin/env python3
# -*- coding: utf-8 -*-

from docx import Document
from docx.shared import Pt, Cm
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml.ns import qn
from docx.oxml import OxmlElement

def create_practice_report():
    doc = Document()

    # Настройка стилей по умолчанию (Times New Roman, 14pt, интервал 1.5)
    style = doc.styles['Normal']
    font = style.font
    font.name = 'Times New Roman'
    font.size = Pt(14)
    font._element.rPr.rFonts.set(qn('w:eastAsia'), 'Times New Roman')
    
    # Установка межстрочного интервала 1.5
    def set_line_spacing(paragraph, spacing_val):
        pPr = paragraph._element.get_or_add_pPr()
        spacing = OxmlElement('w:spacing')
        spacing.set(qn('w:lineRule'), 'auto')
        spacing.set(qn('w:line'), str(spacing_val))
        pPr.append(spacing)

    # Заголовок работы
    title = doc.add_paragraph()
    title.alignment = WD_ALIGN_PARAGRAPH.CENTER
    run = title.add_run("ОТЧЕТ О ПРЕДДИПЛОМНОЙ ПРАКТИКЕ")
    run.bold = True
    run.font.size = Pt(16)
    set_line_spacing(title, 360) # 1.5 * 240 twips

    subtitle = doc.add_paragraph()
    subtitle.alignment = WD_ALIGN_PARAGRAPH.CENTER
    run = subtitle.add_run("Разработка веб-системы инвентарного учета с модулем криптографического аудита на базе проекта Skopidom")
    run.bold = True
    run.font.size = Pt(14)
    set_line_spacing(subtitle, 360)

    doc.add_paragraph() # Пустая строка

    # Введение
    h1 = doc.add_paragraph()
    run = h1.add_run("ВВЕДЕНИЕ")
    run.bold = True
    run.font.size = Pt(14)
    set_line_spacing(h1, 360)

    intro_text = (
        "Целью данной преддипломной практики являлось закрепление теоретических знаний, полученных в ходе научно-исследовательской работы, "
        "путем практической реализации полнофункционального веб-приложения для автоматизированного инвентарного учета. "
        "В рамках практики решалась задача создания системы, обеспечивающей не только учет материальных ценностей, но и гарантированную неизменность журналов аудита за счет применения криптографических методов.\n\n"
        "Основные задачи практики включали: развертывание современной среды разработки с использованием контейнеризации; проектирование реляционной схемы данных и настройку типобезопасного доступа к БД; "
        "реализацию серверной логики на языке Go с соблюдением принципов чистой архитектуры; внедрение уникального модуля хеш-цепочек для аудита событий; "
        "разработку реактивного клиентского интерфейса на React; а также комплексное тестирование и подготовку приложения к промышленной эксплуатации."
    )
    p = doc.add_paragraph(intro_text)
    set_line_spacing(p, 360)

    # Глава 1
    h2 = doc.add_paragraph()
    run = h2.add_run("1. ОРГАНИЗАЦИЯ СРЕДЫ РАЗРАБОТКИ И ПРОЕКТИРОВАНИЕ ДАННЫХ")
    run.bold = True
    run.font.size = Pt(14)
    set_line_spacing(h2, 360)

    ch1_p1 = (
        "Работа началась с настройки окружения. Для обеспечения воспроизводимости сборки и изоляции зависимостей был выбран Docker. "
        "Файл docker-compose.yml описывает три основных сервиса: базу данных PostgreSQL 16, бэкенд-сервис на Go и фронтенд-сервис на Node.js. "
        "Для автоматизации рутинных задач (запуск миграций, генерация кода, линтинг, сборка) был создан Makefile, позволяющий управлять жизненным циклом проекта одной командой."
    )
    p = doc.add_paragraph(ch1_p1)
    set_line_spacing(p, 360)

    ch1_p2 = (
        "На этапе проектирования данных была разработана детальная ER-диаграмма. Основные сущности включают: Users (пользователи системы), Buildings (здания), Rooms (помещения), Categories (категории предметов) и Items (сами предметы учета). "
        "Особое внимание уделено связям между сущностями: каждый предмет привязан к категории и конкретному помещению, которое, в свою очередь, принадлежит зданию. "
        "Миграции базы данных были написаны на чистом SQL с использованием инструмента golang-migrate. Это позволило явно контролировать структуру схемы, создавать необходимые индексы для ускорения поиска и внешние ключи для обеспечения ссылочной целостности."
    )
    p = doc.add_paragraph(ch1_p2)
    set_line_spacing(p, 360)

    ch1_p3 = (
        "Для работы с базой данных из кода на Go был выбран инструмент sqlc. В отличие от традиционных ORM, sqlc генерирует типобезопасный Go-код непосредственно из SQL-запросов. "
        "Это подход обеспечивает компиляцию ошибок при неверном SQL еще на этапе сборки, устраняет проблему N+1 запросов и дает полный контроль над производительностью выполняемых операций. "
        "Архитектура бэкенда была организована по принципу Clean Architecture. Код разделен на три уровня: handlers (обработка HTTP-запросов и валидация входных данных), services (бизнес-логика и транзакционное управление) и repository (абстракция доступа к данным). "
        "В качестве роутера использовался chi/v5 — легковесный и идиоматичный инструмент, соответствующий стандарту net/http, что упрощает тестирование и поддержку кода."
    )
    p = doc.add_paragraph(ch1_p3)
    set_line_spacing(p, 360)

    # Глава 2
    h3 = doc.add_paragraph()
    run = h3.add_run("2. РЕАЛИЗАЦИЯ СЕРВЕРНОЙ ЧАСТИ И МОДУЛЯ АУДИТА")
    run.bold = True
    run.font.size = Pt(14)
    set_line_spacing(h3, 360)

    ch2_p1 = (
        "Первым этапом реализации стала система аутентификации и авторизации. Были реализованы эндпоинты регистрации и входа пользователей. "
        "Пароли хранятся в хешированном виде с использованием надежного алгоритма bcrypt с адаптивной стоимостью. "
        "При успешной аутентификации пользователю выдается JWT-токен, содержащийclaims с идентификатором пользователя и его ролью (Admin или Editor). "
        "Промежуточное ПО (middleware) перехватывает входящие запросы, проверяет валидность токена и добавляет информацию о пользователе в контекст запроса, делая ее доступной для всех уровней приложения."
    )
    p = doc.add_paragraph(ch2_p1)
    set_line_spacing(p, 360)

    ch2_p2 = (
        "Бизнес-логика инвентаризации реализована через набор сервисных методов. CRUD-операции для справочников (здания, комнаты, категории) обеспечивают базовое управление структурой склада. "
        "Наиболее сложной частью является жизненный цикл предмета. Логика предусматривает создание новой записи, перемещение предмета между помещениями (с фиксацией старого и нового местоположения), изменение статуса (например, перевод в состояние «Списан» или «В ремонте») и обновление атрибутов. "
        "Также реализован механизм загрузки файлов: изображения предметов и сопроводительные документы сохраняются в выделенное файловое хранилище с генерацией уникальных имен во избежание коллизий, а пути к файлам записываются в БД."
    )
    p = doc.add_paragraph(ch2_p2)
    set_line_spacing(p, 360)

    ch2_p3 = (
        "Ключевым элементом работы стала разработка подсистемы криптографического аудита. Для этого была спроектирована таблица audit_events со следующей структурой: id, event_data (JSON с деталями изменения), timestamp, user_id, prev_hash и current_hash. "
        "Алгоритм формирования хеш-цепочки работает следующим образом: перед записью нового события система считывает хеш последней успешной записи (prev_hash). "
        "Данные текущего события сериализуются в канонический JSON-формат, конкатенируются с prev_hash, и от полученной строки вычисляется хеш SHA-256. Результат записывается в поле current_hash. "
        "Таким образом, каждая запись криптографически связана с предыдущей."
    )
    p = doc.add_paragraph(ch2_p3)
    set_line_spacing(p, 360)

    ch2_p4 = (
        "Гарантия целостности обеспечивается транзакционностью: сохранение бизнес-данных (например, перемещение предмета) и запись в аудит-лог происходят в рамках одной数据库 транзакции. "
        "Если любой из этапов завершается ошибкой, откатываются все изменения. Также реализован специальный эндпоинт верификации, который проходит по всей цепи записей, пересчитывает хеши и сравнивает их с сохраненными значениями. "
        "Любое расхождение (возникшее, например, при прямой модификации БД злоумышленником) немедленно детектируется, и система сигнализирует о нарушении целостности начиная с определенной записи."
    )
    p = doc.add_paragraph(ch2_p4)
    set_line_spacing(p, 360)

    ch2_p5 = (
        "Дополнительно был реализован функционал импорта и экспорта данных. Парсер CSV позволяет массово загружать номенклатуру предметов из внешних систем, что критически важно при первоначальном наполнении базы. "
        "Модуль экспорта формирует отчеты в формате CSV, позволяя выгружать текущее состояние реестра для внешней обработки или передачи в бухгалтерию."
    )
    p = doc.add_paragraph(ch2_p5)
    set_line_spacing(p, 360)

    # Глава 3
    h4 = doc.add_paragraph()
    run = h4.add_run("3. РАЗРАБОТКА КЛИЕНТСКОЙ ЧАСТИ И ИНТЕГРАЦИЯ")
    run.bold = True
    run.font.size = Pt(14)
    set_line_spacing(h4, 360)

    ch3_p1 = (
        "Фронтенд-часть приложения разрабатывалась с использованием современного стека на базе React 18 и TypeScript. Проект был инициирован через Vite, что обеспечило высокую скорость сборки и горячую перезагрузку модулей в процессе разработки. "
        "Для стилизации применен Tailwind CSS, позволяющий быстро верстать адаптивные интерфейсы с помощью утилитарных классов, и библиотека компонентов Radix UI, предоставляющая доступные (accessibility) примитивы для сложных элементов управления."
    )
    p = doc.add_paragraph(ch3_p1)
    set_line_spacing(p, 360)

    ch3_p2 = (
        "Управление серверным состоянием реализовано с помощью библиотеки TanStack Query (React Query). Этот инструмент взял на себя задачи кэширования ответов API, фоновой синхронизации данных, повторных попыток при ошибках сети и оптимистичного обновления интерфейса. "
        "Были созданы кастомные хуки (useItems, useBuildings, useAuditLog), инкапсулирующие логику запросов к соответствующим эндпоинтам. Axios используется в качестве HTTP-клиента; настроены интерцепторы, которые автоматически добавляют JWT-токен из локального хранилища в заголовки всех исходящих запросов."
    )
    p = doc.add_paragraph(ch3_p2)
    set_line_spacing(p, 360)

    ch3_p3 = (
        "В рамках разработки пользовательских интерфейсов были реализованы следующие ключевые страницы. Страница авторизации содержит форму входа с валидацией полей через Zod и обработкой ошибок аутентификации. "
        "Панель управления (Dashboard) отображает сводную статистику: количество зданий, помещений, активных и списанных предметов. Реестр предметов представляет собой таблицу с поддержкой сортировки, фильтрации по категориям и помещениям, а также поиском по названию. "
        "Для создания и редактирования записей используются модальные окна, что позволяет не покидать контекст списка."
    )
    p = doc.add_paragraph(ch3_p3)
    set_line_spacing(p, 360)

    ch3_p4 = (
        "Карточка предмета содержит подробную информацию об объекте, историю его перемещений и галерею загруженных фотографий. Отдельного внимания заслуживает страница журнала аудита. "
        "Здесь события визуализируются в виде хронологической ленты. Каждая запись сопровождается индикатором статуса целостности: если хеш-цепочка не нарушена, отображается зеленый маркер; в случае детектирования несоответствия хешей — красный, с указанием конкретного блока, где произошла первая модификация."
    )
    p = doc.add_paragraph(ch3_p4)
    set_line_spacing(p, 360)

    ch3_p5 = (
        "Для улучшения пользовательского опыта внедрена система уведомлений. Глобальный обработчик ошибок перехватывает исключения API и показывает всплывающие сообщения (toasts) с описанием проблемы. "
        "Успешные операции (сохранение предмета, импорт данных) также подтверждаются соответствующими уведомлениями. Строгая типизация TypeScript на уровне фронтенда гарантирует, что данные, получаемые от сервера, соответствуют ожидаемым интерфейсам, минимизируя риски runtime-ошибок."
    )
    p = doc.add_paragraph(ch3_p5)
    set_line_spacing(p, 360)

    # Глава 4
    h5 = doc.add_paragraph()
    run = h5.add_run("4. ТЕСТИРОВАНИЕ, ОТЛАДКА И ПОДГОТОВКА К РАЗВЕРТЫВАНИЮ")
    run.bold = True
    run.font.size = Pt(14)
    set_line_spacing(h5, 360)

    ch4_p1 = (
        "Этап тестирования включал несколько уровней проверки. Для бэкенда были написаны модульные тесты, покрывающие ключевые функции сервисного слоя: логику хеширования, проверку прав доступа и валидацию входных данных. "
        "Интеграционные тесты проверяли корректность работы API эндпоинтов в связке с реальной базой данных (поднимаемой в тестовом контейнере)."
    )
    p = doc.add_paragraph(ch4_p1)
    set_line_spacing(p, 360)

    ch4_p2 = (
        "Особое внимание уделено проверке безопасности и целостности. Была проведена эмуляция атаки: вручную изменена запись в таблице audit_events в обход приложения. "
        "Запуск процедуры верификации успешно выявил нарушение: хеш измененной записи не совпал с вычисленным, а цепочка последующих записей также оказалась невалидной. "
        "Проверка защиты от несанкционированного доступа подтвердила, что запросы без JWT-токена или с токеном пользователя низкой роли блокируются на уровне middleware."
    )
    p = doc.add_paragraph(ch4_p2)
    set_line_spacing(p, 360)

    ch4_p3 = (
        "Финальная сборка проекта выполнена с оптимизацией Docker-образов. Для бэкенда использована мультистейдж сборка: компиляция происходит в полном образе с SDK, а рантайм-образ содержит только скомпилированный бинарный файл и корневые сертификаты, что уменьшило итоговый размер образа более чем в 5 раз. "
        "Фронтенд собирается в статические файлы, которые обслуживаются легковесным веб-сервером внутри контейнера. Команда docker-compose up --build успешно разворачивает всю систему локально."
    )
    p = doc.add_paragraph(ch4_p3)
    set_line_spacing(p, 360)

    # Заключение
    h6 = doc.add_paragraph()
    run = h6.add_run("ЗАКЛЮЧЕНИЕ")
    run.bold = True
    run.font.size = Pt(14)
    set_line_spacing(h6, 360)

    conclusion_text = (
        "В ходе прохождения преддипломной практики было разработано полнофункциональное веб-приложение для инвентарного учета, полностью соответствующее поставленным задачам. "
        "Главным результатом работы стала успешная реализация модуля криптографического аудита, который обеспечивает математически доказуемую неизменность журнала событий без необходимости использования полноценных блокчейн-сетей, сохраняя при этом высокую производительность реляционной СУБД.\n\n"
        "В процессе разработки были применены современные инженерные практики: типобезопасность на всех уровнях стека (Go + TypeScript), контейнеризация сервисов, реактивный подход к построению интерфейсов и автоматизация процессов сборки и тестирования. "
        "Полученная система готова к использованию в качестве основы для выпускной квалификационной работы, а также обладает потенциалом для внедрения в реальную эксплуатацию на предприятиях, требующих высокого уровня доверия к данным учета."
    )
    p = doc.add_paragraph(conclusion_text)
    set_line_spacing(p, 360)

    # Сохранение файла
    file_path = "/workspace/Prediplomnaya_Praktika.docx"
    doc.save(file_path)
    return file_path

if __name__ == "__main__":
    path = create_practice_report()
    print(f"Document created successfully at: {path}")
